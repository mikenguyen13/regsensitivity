% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{regsen_bounds}
\alias{regsen_bounds}
\title{Bounds on a regression coefficient under omitted-variable bias}
\usage{
regsen_bounds(
  formula,
  data,
  analysis = c("dmp", "oster"),
  compare = NULL,
  nocompare = NULL,
  rxbar = NULL,
  rybar = Inf,
  cbar = 1,
  rybar_expr = NULL,
  delta = NULL,
  r2long = 1,
  maxovb = NA,
  delta_type = c("eq", "bound"),
  r2long_type = c("eq", "relative"),
  maxovb_type = c("bound", "relative"),
  beta = "sign",
  product = TRUE,
  ngrid = 200L,
  subset = NULL
)
}
\arguments{
\item{formula}{Two-sided formula: \code{y ~ x + w1 + w2 + ...}. The first
right-hand-side variable is the primary independent variable; the rest
are controls.}

\item{data}{A data.frame.}

\item{analysis}{Which sensitivity analysis to run: \code{"dmp"} (default) or
\code{"oster"}.}

\item{compare}{Optional character vector of variables to use as the
comparison set. Defaults to all controls if neither \code{compare} nor
\code{nocompare} is given.}

\item{nocompare}{Optional character vector of controls to \emph{exclude} from
the comparison set.}

\item{rxbar, rybar, cbar}{(DMP) Numeric vectors of sensitivity-parameter
values to sweep over. \code{rybar = Inf} (the default) gives the no-rybar
case; setting it finite invokes the global-optimization code path.}

\item{rybar_expr}{(DMP) A function \code{function(rxbar) rybar} to set rybar
as a function of rxbar (the only supported form in the Stata source
is \code{rybar = rxbar}, i.e. \code{function(rxbar) rxbar}).}

\item{delta, r2long, maxovb}{(Oster) Numeric vectors of sensitivity values.}

\item{delta_type}{One of \code{"eq"} (equality, the default) or \code{"bound"}.}

\item{r2long_type}{One of \code{"eq"} (the default) or \code{"relative"}. When
\code{"relative"}, values are multiplied by R-squared(medium).}

\item{maxovb_type}{One of \code{"bound"} (default) or \code{"relative"}. When
\code{"relative"}, values are multiplied by |Beta(medium)|.}

\item{beta}{Hypothesis spec for the breakdown point. See \code{\link[=regsen_breakdown]{regsen_breakdown()}}.}

\item{product}{Logical. If \code{TRUE} (default), all combinations of the
sensitivity-parameter grids are evaluated; if \code{FALSE}, the inputs are
zipped element-wise. Maps to Stata's \code{noproduct} option (inverted).}

\item{ngrid}{Resolution of the finer grid stored in the result. Default 200.}

\item{subset}{Optional logical or integer vector indicating which rows
of \code{data} to include in the estimation.}
}
\value{
A \code{regsensitivity} object. The \code{results} field holds a data.frame
with one row per sensitivity-parameter point.
}
\description{
Computes the identified set for the coefficient on the primary independent
variable in the infeasible long regression, across a grid of sensitivity
parameters. Implements the analyses of Diegert, Masten & Poirier (2026)
(the default) and of Oster (2019) extended by Masten & Poirier (2026).
}
\examples{
\donttest{
data(bfg2020)
bnds <- regsen_bounds(
  avgrep2000to2016 ~ tye_tfe890_500kNI_100_l6 +
    log_area_2010 + lat + lon + temp_mean + rain_mean + elev_mean +
    d_coa + d_riv + d_lak + ave_gyi,
  data = bfg2020,
  cbar = 0.1
)
print(bnds)
}
}
