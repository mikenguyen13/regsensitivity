% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_partial_r2}
\alias{calibrate_partial_r2}
\title{Pairwise partial R-squared of calibration covariates}
\usage{
calibrate_partial_r2(
  formula,
  data,
  compare = NULL,
  nocompare = NULL,
  subset = NULL
)
}
\arguments{
\item{formula}{Two-sided formula: \code{y ~ x + w1 + w2 + ...}. The first
right-hand-side variable is the primary independent variable; the rest
are controls.}

\item{data}{A data.frame.}

\item{compare}{Optional character vector of variables to use as the
comparison set. Defaults to all controls if neither \code{compare} nor
\code{nocompare} is given.}

\item{nocompare}{Optional character vector of controls to \emph{exclude} from
the comparison set.}

\item{subset}{Optional logical or integer vector indicating which rows
of \code{data} to include in the estimation.}
}
\value{
A data.frame with columns \code{variable} and \code{R2}.
}
\description{
For each \code{W1[k]}, computes
\deqn{R^2_{W_{1k} \sim W_{1,-k} \cdot W_0}}
i.e. the R-squared of regressing the \code{W0}-residualized \code{W1[k]} on the
\code{W0}-residualized \code{W1[-k]}. Matches Table 3 in DMP (2026): a quick read
on how collinear the comparison controls are.
}
