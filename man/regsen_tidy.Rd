% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{regsen_tidy}
\alias{regsen_tidy}
\alias{regsen_glance}
\title{Tidy a sensitivity analysis}
\usage{
regsen_tidy(x, conf.int = TRUE, ...)

regsen_glance(x, ...)
}
\arguments{
\item{x}{A \code{regsensitivity} object.}

\item{conf.int}{Ignored; present for signature compatibility with other
tidiers. Sensitivity bounds are not confidence intervals -- see
\code{\link[=regsen_boot]{regsen_boot()}} for sampling uncertainty in the breakdown point.}

\item{...}{Ignored.}
}
\value{
\code{regsen_tidy()} returns a \code{data.frame} with the sensitivity
parameters plus \code{conf.low}/\code{conf.high} holding the identified set, using
broom's column names so downstream packages recognise them.
\code{regsen_glance()} returns a one-row \code{data.frame}.
}
\description{
A \pkg{broom}-style tidier. \code{tidy()} returns one row per point of the
sensitivity sweep; \code{glance()} returns a one-row summary of the analysis
as a whole.
}
\details{
Implementing these makes \code{regsensitivity} objects usable by anything
that speaks the broom vocabulary -- most usefully \pkg{modelsummary},
which is how many economists assemble their tables.

The methods are registered on \pkg{generics}' \code{tidy()} and \code{glance()}
only when that package is installed, so it stays an optional dependency.
Without it, call \code{\link[=regsen_tidy]{regsen_tidy()}} and \code{\link[=regsen_glance]{regsen_glance()}} directly.
}
\examples{
\donttest{
data(bfg2020)
res <- regsen_bounds(
    avgrep2000to2016 ~ tye_tfe890_500kNI_100_l6 + log_area_2010 + lat + lon,
    data = bfg2020, compare = c("log_area_2010", "lat", "lon"),
    cbar = c(0.1, 0.5)
)
head(regsen_tidy(res))
regsen_glance(res)
}
}
