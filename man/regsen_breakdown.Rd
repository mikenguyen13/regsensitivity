% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{regsen_breakdown}
\alias{regsen_breakdown}
\title{Breakdown frontier for a regression coefficient hypothesis}
\usage{
regsen_breakdown(
  formula,
  data,
  analysis = c("dmp", "oster"),
  compare = NULL,
  nocompare = NULL,
  cbar = 1,
  rybar = Inf,
  rybar_expr = NULL,
  r2long = 1,
  maxovb = NA,
  r2long_type = c("eq", "relative"),
  maxovb_type = c("bound", "relative"),
  beta = "sign",
  ngrid = 200L,
  subset = NULL
)
}
\arguments{
\item{formula}{Two-sided formula: \code{y ~ x + w1 + w2 + ...}. The first
right-hand-side variable is the primary independent variable; the rest
are controls.}

\item{data}{A data.frame.}

\item{analysis}{Which sensitivity analysis to run: \code{"dmp"} (default) or
\code{"oster"}.}

\item{compare}{Optional character vector of variables to use as the
comparison set. Defaults to all controls if neither \code{compare} nor
\code{nocompare} is given.}

\item{nocompare}{Optional character vector of controls to \emph{exclude} from
the comparison set.}

\item{cbar, rybar, rybar_expr}{(DMP) Same as in \code{\link[=regsen_bounds]{regsen_bounds()}}.}

\item{r2long, maxovb}{(Oster) Same as in \code{\link[=regsen_bounds]{regsen_bounds()}}.}

\item{r2long_type}{One of \code{"eq"} (the default) or \code{"relative"}. When
\code{"relative"}, values are multiplied by R-squared(medium).}

\item{maxovb_type}{One of \code{"bound"} (default) or \code{"relative"}. When
\code{"relative"}, values are multiplied by |Beta(medium)|.}

\item{beta}{Hypothesis spec. One of:
\itemize{
\item \code{"sign"} -- the hypothesis that sign(beta_long) = sign(beta_med).
\item a numeric scalar or vector. Use the helpers \code{\link[=bnd_lb]{bnd_lb()}}, \code{\link[=bnd_ub]{bnd_ub()}},
\code{\link[=bnd_eq]{bnd_eq()}} to set the direction, e.g. \code{beta = bnd_lb(0)} for the
hypothesis \code{beta > 0}.
}}

\item{ngrid}{Resolution of the finer grid stored in the result. Default 200.}

\item{subset}{Optional logical or integer vector indicating which rows
of \code{data} to include in the estimation.}
}
\value{
A \code{regsensitivity} object.
}
\description{
Find the smallest sensitivity-parameter value at which a given hypothesis
about the long-regression coefficient first fails. For DMP, this is rxbar
as a function of (cbar, rybar, beta). For Oster, this is |delta| as a
function of R-squared(long), beta and (optionally) maxovb.
}
\examples{
\donttest{
data(bfg2020)
bk <- regsen_breakdown(
  avgrep2000to2016 ~ tye_tfe890_500kNI_100_l6 +
    log_area_2010 + lat + lon + temp_mean + rain_mean + elev_mean +
    d_coa + d_riv + d_lak + ave_gyi,
  data = bfg2020,
  cbar = seq(0, 1, 0.1)
)
print(bk)
}
}
