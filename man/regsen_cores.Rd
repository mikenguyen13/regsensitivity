% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{regsen_cores}
\alias{regsen_cores}
\title{Cores used by the package}
\usage{
regsen_cores(n = NULL)
}
\arguments{
\item{n}{One of:
\itemize{
\item \code{NULL} (default): read the current setting without changing it.
\item a positive integer: use that many cores.
\item \code{"auto"}: use \code{parallel::detectCores() - 2}, and at least 1. Two are
left free so the session, and whatever else the machine is doing,
stay responsive.
}

Whatever is set, at most 2 are used while \verb{R CMD check --as-cran} is
running, which forbids more.}
}
\value{
The number of cores in effect after the call, invisibly when
\code{n} is given.
}
\description{
Sets or reads the number of cores that \code{\link[=regsen_bounds]{regsen_bounds()}},
\code{\link[=regsen_breakdown]{regsen_breakdown()}}, \code{\link[=regsen_multi]{regsen_multi()}} and \code{\link[=regsen_boot]{regsen_boot()}} use by
default. Each of those also takes an \code{ncores} argument for a single
call; this sets the session-wide default those arguments fall back to,
held in \code{options(regsensitivity.ncores)}.
}
\details{
Parallel runs return exactly what serial runs return. The identified set
and the breakdown frontiers draw no random numbers, and the bootstrap
seeds each replicate from a vector drawn once up front, so nothing
depends on how the work was divided.

On macOS and Linux the work is forked with \code{\link[parallel:mclapply]{parallel::mclapply()}}, so the
data and model need no copying. Windows has no fork; it gets a PSOCK
cluster, which is slower to start and pays to ship the data once, so
there the gain is only worth having for long jobs (a bootstrap, or a
finite-\code{rybar} sweep with many grid points).

What is parallel: the grid points of a \code{\link[=regsen_bounds]{regsen_bounds()}} sweep (only the
finite-\code{rybar} regime costs anything; the closed forms are cheap either
way), the values of a \code{\link[=regsen_breakdown]{regsen_breakdown()}} frontier, the treatments of
\code{\link[=regsen_multi]{regsen_multi()}}, and the replicates and jackknife of \code{\link[=regsen_boot]{regsen_boot()}}.
Work inside a replicate or a treatment is run serially, so cores are
never oversubscribed by nesting.
}
\examples{
regsen_cores()          # the current setting, 1 unless changed
\dontrun{
regsen_cores("auto")    # all but two of the machine's cores
regsen_cores(4)
regsen_cores(1)         # back to serial
}
}
