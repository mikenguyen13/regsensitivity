% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{regsen_boot}
\alias{regsen_boot}
\title{Bootstrap confidence interval for the breakdown point}
\usage{
regsen_boot(
  formula,
  data,
  ...,
  R = 999L,
  cluster = NULL,
  level = 0.95,
  seed = NULL,
  show_progress = interactive()
)
}
\arguments{
\item{formula}{Two-sided formula: \code{y ~ x + w1 + w2 + ...}. The first
right-hand-side variable is the primary independent variable; the rest
are controls.}

\item{data}{A data.frame.}

\item{...}{Additional arguments forwarded to \code{\link[=regsen_breakdown]{regsen_breakdown()}} (the
analysis to bootstrap).}

\item{R}{Integer. Number of bootstrap replications. Defaults to 999.}

\item{cluster}{Optional character scalar naming a column of \code{data} to
resample at the cluster level (e.g. \code{"km_grid_cel_code"} for the BFG
2020 application). When NULL the standard non-parametric bootstrap is
used.}

\item{level}{Two-sided confidence level for the percentile CI. Default
0.95.}

\item{seed}{Optional integer seed for reproducibility.}

\item{show_progress}{Logical; print progress bar.}
}
\value{
An object of class \code{regsensitivity_boot} containing:
\code{point}, \code{replicates}, \code{ci}, \code{level}, \code{R}, \code{cluster}, \code{na}.
}
\description{
Computes a non-parametric (or cluster) bootstrap percentile confidence
interval for the breakdown point returned by \code{\link[=regsen_breakdown]{regsen_breakdown()}} or the
scalar \verb{$breakdown} field of \code{\link[=regsen_bounds]{regsen_bounds()}}.
}
\details{
For DMP analyses the breakdown point is computed exactly as in
\code{\link[=regsen_breakdown]{regsen_breakdown()}}; when \code{rxbar}, \code{rybar} and \code{cbar} are all scalar the
returned breakdown is the rxbar breakdown for the (scalar) hypothesis on
beta. For Oster analyses the breakdown is the |delta| value at which
the hypothesis first fails.
}
\examples{
\donttest{
data(bfg2020)
bfg2020$statea <- factor(bfg2020$statea)
w1 <- c("log_area_2010", "lat", "lon", "temp_mean", "rain_mean",
        "elev_mean", "d_coa", "d_riv", "d_lak", "ave_gyi")
form <- reformulate(c("tye_tfe890_500kNI_100_l6", w1, "statea"),
                    response = "avgrep2000to2016")
set.seed(1)
bb <- regsen_boot(form, bfg2020, compare = w1, cbar = 1,
                   R = 199, cluster = "km_grid_cel_code")
print(bb)
}
}
