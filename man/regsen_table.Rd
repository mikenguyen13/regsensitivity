% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{regsen_table}
\alias{regsen_table}
\title{Render a sensitivity analysis as a publication-ready table}
\usage{
regsen_table(
  x,
  format = NULL,
  digits = 3,
  caption = NULL,
  label = NULL,
  notes = NULL,
  booktabs = TRUE,
  threeparttable = TRUE,
  align = NULL,
  col_names = NULL,
  escape = TRUE,
  max_rows = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{regsensitivity} object.}

\item{format}{One of \code{"latex"}, \code{"html"}, \code{"markdown"}, \code{"pipe"} or
\code{"simple"}. Defaults to \code{"latex"} when knitting to PDF and
\code{"markdown"} otherwise.}

\item{digits}{Significant digits for the numeric columns.}

\item{caption}{Table caption. \code{NULL} for none.}

\item{label}{LaTeX label, used as \verb{\\\\label\{tab:<label>\}}. Ignored for
non-LaTeX formats.}

\item{notes}{Character vector of table notes, one element per line. Set
to \code{NULL} for none, or \code{TRUE} to use an automatically generated note
recording the analysis, hypothesis and sample size -- which is the
information a referee will look for.}

\item{booktabs}{Logical; use \code{booktabs} rules in LaTeX output.}

\item{threeparttable}{Logical; wrap LaTeX output and its notes in a
\code{threeparttable} environment. Requires \verb{\\\\usepackage\{threeparttable\}}
in the document preamble.}

\item{align}{Column alignment string, e.g. \code{"lrrr"}. \code{NULL} left-aligns
character columns and right-aligns numeric ones.}

\item{col_names}{Optional character vector of column headings. \code{NULL}
uses tidied versions of the internal names.}

\item{escape}{Logical; escape special characters. Set \code{FALSE} if you are
supplying LaTeX markup in \code{col_names} or \code{caption}.}

\item{max_rows}{Optional integer. If the sweep is longer than this, the
table is thinned to \code{max_rows} evenly spaced rows and a note records
that this happened. \code{NULL} keeps every row.}

\item{...}{Passed to \code{\link[knitr:kable]{knitr::kable()}}.}
}
\value{
A \code{knitr_kable} object, which prints as the requested markup and
renders directly in an Rmd/Qmd chunk.
}
\description{
Formats the results of \code{\link[=regsen_bounds]{regsen_bounds()}} or \code{\link[=regsen_breakdown]{regsen_breakdown()}} for a
manuscript. The \code{format} argument selects the output flavour, so the same
call works in an Rmd/Qmd document knitting to PDF, to HTML, or to a
Markdown target such as GitHub or Typst.
}
\details{
For LaTeX the default follows the convention most economics journals
expect: \code{booktabs} rules, and table notes wrapped in a \code{threeparttable}
environment so the note block is set to the width of the table rather
than the width of the page. Both are switchable.

Anything this function does not expose can be done by taking
\code{\link[=as.data.frame]{as.data.frame()}} of the object and passing it to the table package of
your choice; nothing here is a dead end.
}
\examples{
\donttest{
data(bfg2020)
res <- regsen_bounds(
    avgrep2000to2016 ~ tye_tfe890_500kNI_100_l6 + log_area_2010 + lat + lon,
    data = bfg2020, compare = c("log_area_2010", "lat", "lon"),
    cbar = c(0.1, 0.5, 1)
)
regsen_table(res, format = "markdown", digits = 3)
regsen_table(res, format = "latex", notes = TRUE,
             caption = "Identified sets", label = "idset")
}
}
