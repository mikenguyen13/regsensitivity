% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_rho}
\alias{calibrate_rho}
\title{Calibration parameters rho_k for rxbar}
\usage{
calibrate_rho(formula, data, compare = NULL, nocompare = NULL, subset = NULL)
}
\arguments{
\item{formula}{Two-sided formula: \code{y ~ x + w1 + w2 + ...}. The first
right-hand-side variable is the primary independent variable; the rest
are controls.}

\item{data}{A data.frame.}

\item{compare}{Optional character vector of variables to use as the
comparison set. Defaults to all controls if neither \code{compare} nor
\code{nocompare} is given.}

\item{nocompare}{Optional character vector of controls to \emph{exclude} from
the comparison set.}

\item{subset}{Optional logical or integer vector indicating which rows
of \code{data} to include in the estimation.}
}
\value{
A data.frame with columns \code{variable} and \code{rho} (a percentage).
}
\description{
For each calibration covariate \code{W1[k]}, computes
\deqn{\rho_k = \frac{\sqrt{\mathrm{Var}(\pi_{1,med,k} W_{1k})}}{
                      \sqrt{\mathrm{Var}(\pi_{1,med,-k}' W_{1,-k})}},}
where pi(1,med) is the OLS coefficient on W1 from the regression of
the treatment X on (W0, W1). Variances are computed on the
W0-residualized variables, matching the construction in DMP (2026)
equation (3.5).
}
\details{
These are point-identified reference values to compare the rxbar
breakdown point against. See DMP (2026) section 3.4 and Table 4.
}
\examples{
\donttest{
data(bfg2020)
bfg2020$statea <- factor(bfg2020$statea)
w1 <- c("log_area_2010", "lat", "lon", "temp_mean", "rain_mean",
        "elev_mean", "d_coa", "d_riv", "d_lak", "ave_gyi")
form <- reformulate(c("tye_tfe890_500kNI_100_l6", w1, "statea"),
                    response = "avgrep2000to2016")
calibrate_rho(form, bfg2020, compare = w1)
}
}
