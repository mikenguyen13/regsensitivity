% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi.R
\name{regsen_multi}
\alias{regsen_multi}
\alias{plot.regsensitivity_multi}
\title{Sensitivity analysis for several treatments at once}
\usage{
regsen_multi(
  formula,
  data,
  treatments,
  compare = NULL,
  fun = regsen_breakdown,
  ...
)

\method{plot}{regsensitivity_multi}(x, base_size = 11, ...)
}
\arguments{
\item{formula}{A model formula. Its right-hand side must contain every
name in \code{treatments}. Which term appears first does not matter here,
since each treatment is promoted in turn.}

\item{data}{A data frame.}

\item{treatments}{Character vector of variables to treat as the
treatment in turn.}

\item{compare}{Passed to the underlying analysis. A variable currently
acting as the treatment is removed from \code{compare} for that row, since
a variable cannot calibrate against itself.}

\item{fun}{The analysis to run: \code{\link[=regsen_breakdown]{regsen_breakdown()}} (default) or
\code{\link[=regsen_bounds]{regsen_bounds()}}.}

\item{...}{Passed to \code{fun}, e.g. \code{cbar}, \code{analysis}, \code{beta}.}

\item{x}{A \code{regsensitivity_multi} object.}

\item{base_size}{Base font size, passed to \code{\link[=theme_regsen]{theme_regsen()}}.}
}
\value{
A \code{data.frame} of class \code{regsensitivity_multi}, one row per
treatment, with columns \code{treatment}, \code{estimate} (the medium-regression
coefficient), \code{breakdown}, and \code{n}. Rows where the analysis failed
carry \code{NA} and an \code{error} column explaining why, rather than aborting
the whole sweep.
}
\description{
Runs \code{\link[=regsen_breakdown]{regsen_breakdown()}} (or \code{\link[=regsen_bounds]{regsen_bounds()}}) once per candidate
treatment and collects the breakdown points into one table. This is the
applied question "which of my regressors would survive an unobservable,
and which would not?", answered in a single call.
}
\details{
Each treatment is analysed in the \emph{same} model: the variable of interest
moves to the front of the formula and every other right-hand-side term
stays a control, including the other candidate treatments. That is
deliberate -- dropping the others would change the specification and the
breakdown points would no longer be comparable across rows.
}
\examples{
\donttest{
data(bfg2020)
w1 <- c("log_area_2010", "lat", "lon")
regsen_multi(
    avgrep2000to2016 ~ tye_tfe890_500kNI_100_l6 + log_area_2010 + lat + lon,
    data = bfg2020,
    treatments = c("tye_tfe890_500kNI_100_l6", "lat"),
    compare = w1, cbar = 1
)
}
}
